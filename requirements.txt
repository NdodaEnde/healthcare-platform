flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7
gunicorn==21.2.0
python-dotenv==1.0.0
openai==1.12.0  # For OpenAI API integration
numpy==1.26.0  # Required for image processing
Pillow==10.2.0  # Required for image processing
# Install core dependencies first
PyMuPDF>=1.25.5,<2.0.0  # Version compatible with agentic-doc
# Landing AI SDK options (uncomment the one you need)
# landingai==0.0.17  # Older version
agentic-doc==0.1.3  # Newer version renamed to agentic-doc